let table = [];
function displayArray(array) {
    for(let data of table) {
        console.log(data);
    }
}

table = [1, 2, 3, 4, 5];
displayArray();
table = [10, 20, 30, 40, 50];
displayArray();
table = [5, 4, 3, 2, 1];
displayArray();
table = [100, 200, 300, 400, 500];
displayArray();